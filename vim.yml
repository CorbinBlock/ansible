---
 - name: Ansible apt module examples
   hosts: all
   become: true
   tasks: 
    - name: Ansible Update Cache and install module
      apt:
        name: vim
        state: latest
        update_cache: yes
