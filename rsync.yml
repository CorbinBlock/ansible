---
    - name: Rsync - Dev
      hosts: localhost
      tasks: 
       - name: Rsync - Dev
         ansible.builtin.shell: rsync --archive --exclude '.git' --progress --rsh 'ssh -p 50200' --verbose '/home/cblock/.local/share/docs/' 'cblock@devenvzone.com:/home/cblock/.local/share/docs/'
    - name: Rsync - Prod
      hosts: localhost
      tasks: 
       - name: Rsync - Prod
         ansible.builtin.shell: rsync --archive --exclude '.git' --progress --rsh 'ssh -p 50100' --verbose '/home/cblock/.local/share/docs/' 'cblock@devenvzone.com:/home/cblock/.local/share/docs/'